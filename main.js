



















//const imagePreview = document.getElementById('img-preview');

//const imageUploader = document.getElementById('img-uploader');

// capturar el evento del usuario al subir imagen.
// addEventListener evento change es cuando nosotros seleccionamos una imagen
// realmente me da un ubjeto con toda su informacion (metadatos).
//imageUploader.addEventListener('change', (e) => {
//    const file = e.target.files[0];

    // donde enviar el archivo

    // previsualisando
//    console.log(file);

//});
